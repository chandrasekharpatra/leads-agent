{
	"name": "leads-ui",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "npm run build && wrangler dev",
		"postinstall": "nuxt prepare",
		"deploy": "npm run build && wrangler deploy --var NUXT_SESSION_PASSWORD:${NUXT_SESSION_PASSWORD}",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@nuxt/ui": "^4.1.0",
		"nuxt": "^4.1.3",
		"nuxt-auth-utils": "^0.5.25",
		"vue": "^3.5.22",
		"vue-router": "^4.6.3"
	},
	"devDependencies": {
		"@iconify-json/lucide": "^1.2.71",
		"nitro-cloudflare-dev": "^0.2.2",
		"nitropack": "^2.12.9",
		"wrangler": "^4.45.1"
	}
}
